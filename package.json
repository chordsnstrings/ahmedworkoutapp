{
  "name": "ocpp-platform",
  "private": true,
  "version": "0.1.0",
  "description": "An OCPP 1.6J / 2.0.1 Central System (CSMS) with a live web dashboard.",
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w server && npm run build -w web",
    "lint": "npm run lint -w server && npm run lint -w web",
    "simulator": "npm run simulator -w server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
